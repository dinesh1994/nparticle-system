#include "Vertex.h"

int verticeCount;

int Vertex::getId(){ return id; }

Vertex::Vertex(){
  id = verticeCount;
  verticeCount++;
}

Vertex::Vertex(float x, float y, float z)
{
    pos = Vec3f(x, y, z);
    id = verticeCount;
    verticeCount++;
}

